pipeline {
    agent any
    
    stages {
        stage('Print Branch Name') {
            steps {
                script {
                    echo "frontend"
                    echo "docker build"
                    echo "kubernetes deployment on ec2-instance"
                }
            }
        }
        
    }
}
