pipeline {
    agent any
    
    stages {
        stage('Print Branch Name') {
            steps {
                script {
                    echo "backend branch"
                    echo "docker build"
                    echo "kubernetes deploy on EKS"
                }
            }
        }
        
    }
}
