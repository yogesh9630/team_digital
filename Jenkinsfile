pipeline {
    agent any
    
    stages {
        stage('Print Branch Name') {
            steps {
                script {
                    echo "backend branch"
                }
            }
        }
        
        // Add more stages as needed
    }
}
